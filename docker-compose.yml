version: '3.8'

services:
  web:
    container_name: voltorb-flip
    build: .
    ports:
      - '3002:3002'
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true
